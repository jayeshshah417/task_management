<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.TaskManagement" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.TaskManagement" parent="Base.Theme.TaskManagement" />

    <style name="TextViewHeader" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/textHeader</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:layout_marginTop">@dimen/Fieldmargin</item>
    </style>
    <style name="TextViewBody" parent="Widget.AppCompat.TextView">
        <item name="android:textSize">@dimen/textBody</item>
        <item name="android:textColor">@color/textColor1</item>
        <item name="android:layout_marginTop">@dimen/Fieldmargin</item>
    </style>
</resources>